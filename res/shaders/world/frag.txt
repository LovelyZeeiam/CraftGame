#version 330

in vec2 otexPos;
in vec3 ocolor;

uniform sampler2D texSampler;

out vec4 out_color;

void main(){
	vec4 color = texture(texSampler, otexPos) * vec4(ocolor, 1.0);
	if(color.w == 0) discard;
	out_color = color;

	
}