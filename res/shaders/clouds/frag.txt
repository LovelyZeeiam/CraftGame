#version 330

in vec2 otexPos;
in vec3 ocolor;

uniform sampler2D texSampler;
uniform float cloud_alpha;

out vec4 out_color;

void main(){
	out_color = texture(texSampler, otexPos) * vec4(ocolor, cloud_alpha);

	
}